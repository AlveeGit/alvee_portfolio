/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class", // Enables dark mode with class
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
      // You can add custom colors, spacing, etc.
    },
  },
  plugins: [],
};
